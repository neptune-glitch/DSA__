class Solution {
    public boolean isPalindrome(int x) {
        if (x<0){
            return false;
        }
        int num=x;
        int sum=0;
        while (num>0){
            int rem=num%10;
            sum=10*sum+rem;
            num=num/10;

        
        }
        if (sum==x) return true;
        else return false;
    }
}
